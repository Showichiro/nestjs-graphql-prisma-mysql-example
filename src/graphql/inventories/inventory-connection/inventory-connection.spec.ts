import { InventoryConnection } from './inventory-connection';

describe('InventoryConnection', () => {
  it('should be defined', () => {
    expect(new InventoryConnection()).toBeDefined();
  });
});
